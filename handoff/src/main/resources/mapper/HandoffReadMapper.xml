<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.handoff.repository.HandoffReadMapper">

  <insert id="upsertRead">
    INSERT INTO handoff_reads (handoff_id, user_id, read_at)
    VALUES (#{handoffId}, #{userId}, NOW())
    ON DUPLICATE KEY UPDATE read_at = NOW()
  </insert>
  
  <update id="resetReadByHandoffId">
	  UPDATE handoff_reads
	  SET
	    read_flag = 0,
	    read_at = NULL
	  WHERE handoff_id = #{handoffId}
  </update>
  
    <delete id="deleteByHandoffId">
    DELETE FROM handoff_reads
    WHERE handoff_id = #{handoffId}
  </delete>

</mapper>
