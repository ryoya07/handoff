spring:
  datasource:
    url: jdbc:mysql://localhost:3306/handoff_app?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Tokyo
    username: handoff_user
    password: handoff_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
